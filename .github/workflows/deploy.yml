name: CI/CD Pipeline

on:
  push:
    branches: [ deploy ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Subir código como artefacto
      uses: actions/upload-artifact@v4
      with:
        name: app-code
        path: .

  push:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/download-artifact@v4
      with:
        name: app-code
    - uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: us-east-1
    - uses: aws-actions/amazon-ecr-login@v1
    - run: |
        docker build -t 914091100375.dkr.ecr.us-east-1.amazonaws.com/estadisticas-service:latest .
        docker push 914091100375.dkr.ecr.us-east-1.amazonaws.com/estadisticas-service:latest

  deploy:
    needs: push
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: us-east-1
    - run: aws eks update-kubeconfig --region us-east-1 --name vidal-casino-cluster
    - run: kubectl apply -f k8s/